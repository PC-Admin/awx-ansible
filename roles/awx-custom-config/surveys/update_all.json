{
  "name": "Update All Servers",
  "description": "Checks for updates, then applies those updates to every server connected to AWX/Automation Controller.",
  "spec": [
    {
      "question_name": "Force Upgrade",
      "question_description": "Skips checking the repository, forces updates to every server connected to AWX/Automation Controller.",
      "required": true,
      "min": null,
      "max": null,
      "default": "false",
      "choices": "true\nfalse",
      "new_question": true,
      "variable": "force_upgrade",
      "type": "multiplechoice"
    }
  ]
}

---

- name: "Setup for the AWX/Automation Controller system."
  hosts: "{{ 'awx_servers' }}"
  become: true

  roles:
    #- awx-provision
    - awx-install
    - awx-custom-config
    - awx-backup
    - awx-monitor
    - awx-backup-rehearsal